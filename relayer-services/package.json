{
  "name": "@1inch-cross-chain/relayer-services",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "executor-client",
    "marketplace-api",
    "api-gateway"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/node": "^20.0.0"
  }
}